add test function

add randomized checkbox and timer on/off checkbox

add option for grid size (min/max) with 15x15 max grid size.

??make content pane scrollable

set max and min grid cell sizes

??add high score/time list / score history
	networking?